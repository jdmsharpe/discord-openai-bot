FROM python:3.11

WORKDIR /bot

COPY ./requirements.txt /bot
RUN pip3 install --no-cache-dir -r requirements.txt

RUN apt-get -y update
RUN apt-get -y upgrade

COPY ./tests /bot/tests

CMD ["python", "tests/test_chatgpt.py"]
